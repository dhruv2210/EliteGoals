import type { ComponentTokenMap } from 'antd/es/theme/interface';
export interface TokenMeta {
    type: string;
    name: string;
    nameEn: string;
    desc: string;
    descEn: string;
    source: 'seed' | 'map' | 'alias' | 'custom' | keyof ComponentTokenMap;
}
export declare type TokenMetaMap = Record<string, TokenMeta>;
export declare type TokenGroup<T> = {
    key: string;
    name: string;
    nameEn: string;
    desc: string;
    descEn: string;
    type?: string;
    seedToken?: T[];
    mapToken?: T[];
    aliasToken?: T[];
    groups?: TokenGroup<T>[];
    mapTokenGroups?: string[];
    aliasTokenDescription?: string;
};
export declare type TokenCategory<T> = {
    name: string;
    nameEn: string;
    desc: string;
    descEn: string;
    groups: TokenGroup<T>[];
};
export declare type TokenTree<T extends string = string> = TokenCategory<T>[];
