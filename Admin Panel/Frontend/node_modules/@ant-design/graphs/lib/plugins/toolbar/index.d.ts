import { Graph, ToolbarCfg } from '../../interface';
export interface IToolbar {
    toolbarCfg: ToolbarCfg;
    graph: Graph;
    container: HTMLDivElement | null;
}
export declare const createToolbar: ({ graph, container, toolbarCfg }: IToolbar) => void;
