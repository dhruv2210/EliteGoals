"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getStatusBBox = void 0;
var get_size_1 = require("./get-size");
var constants_1 = require("../constants");
/**
 * card status 对布局的影响，直接加到 padding 中
 */
var getStatusBBox = function (cfg) {
    if (!cfg) {
        return [0, 0, 0, 0];
    }
    var _a = cfg.size, size = _a === void 0 ? [] : _a, _b = cfg.position, position = _b === void 0 ? 'left' : _b;
    var _c = (0, get_size_1.getSize)(size), width = _c[0], height = _c[1];
    var appendPadding = [];
    switch (position) {
        case 'top':
            appendPadding.push(height !== null && height !== void 0 ? height : constants_1.defaultStatusBarWidth, 0, 0, 0);
            break;
        case 'right':
            appendPadding.push(0, width !== null && width !== void 0 ? width : constants_1.defaultStatusBarWidth, 0, 0);
            break;
        case 'bottom':
            appendPadding.push(0, 0, height !== null && height !== void 0 ? height : constants_1.defaultStatusBarWidth, 0);
            break;
        case 'left':
            appendPadding.push(0, 0, 0, width !== null && width !== void 0 ? width : constants_1.defaultStatusBarWidth);
            break;
    }
    return appendPadding;
};
exports.getStatusBBox = getStatusBBox;
