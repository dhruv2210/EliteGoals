"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
Object.defineProperty(exports, "__esModule", { value: true });
// 合并 defaultProps
var react_1 = require("react");
var utils_1 = require("../utils");
function useProps(props, defaultProps) {
    var cloneProps = (0, utils_1.deepClone)(props);
    var mergeProps = (0, react_1.useCallback)(function (p, defaultProps) {
        var config = __assign({}, defaultProps);
        var propsKeys = Object.keys(p);
        propsKeys.forEach(function (key) {
            if ((0, utils_1.getType)(p[key]) === 'Object') {
                config[key] = __assign(__assign({}, defaultProps[key]), p[key]);
            }
            else {
                config[key] = p[key];
            }
        });
        return config;
    }, [props, defaultProps]);
    var uProps = mergeProps(cloneProps, defaultProps);
    return {
        uProps: uProps,
    };
}
exports.default = useProps;
