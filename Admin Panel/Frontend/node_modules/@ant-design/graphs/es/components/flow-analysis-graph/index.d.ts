import React from 'react';
import { DagreLayout } from '@antv/layout';
import { CardItems, CommonConfig, FlowGraphEdgeData, NodeCfg, NodeData, NodeConfig, FetchLoading } from '../../interface';
export declare type FlowAnalysisNodeData = NodeData<{
    title?: string;
    items?: CardItems[];
}>;
export interface FlowAnalysisGraphConfig extends Omit<CommonConfig<DagreLayout>, 'data' | 'nodeCfg'>, FetchLoading {
    data: {
        nodes: FlowAnalysisNodeData[];
        edges: FlowGraphEdgeData[];
    };
    nodeCfg?: NodeCfg & {
        /** 点击展开时异步获取数据 */
        asyncData?: (nodeCfg: NodeConfig) => Promise<{
            nodes: FlowAnalysisNodeData[];
            edges?: FlowGraphEdgeData[];
        }>;
    };
}
declare const FlowAnalysisGraph: React.FC<FlowAnalysisGraphConfig>;
export default FlowAnalysisGraph;
