import React from 'react';
import { Heatmap, HeatmapOptions } from '@antv/l7plot';
import { PlotRef, ContainerConfig } from '../../types';
import { L7PlotConfig } from '../../hooks/useL7Plot';
export interface GeographicHeatmapConfig extends L7PlotConfig<HeatmapOptions, Heatmap>, ContainerConfig {
    chartRef?: PlotRef<Heatmap>;
}
declare const GeographicHeatmap: React.ForwardRefExoticComponent<GeographicHeatmapConfig & React.RefAttributes<unknown>>;
export default GeographicHeatmap;
