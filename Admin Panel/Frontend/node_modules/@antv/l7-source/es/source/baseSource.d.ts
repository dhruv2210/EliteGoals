import { ITileSource } from '../interface';
export default abstract class BaseSource implements ITileSource {
    protected x: number;
    protected y: number;
    protected z: number;
    constructor(data: any, x: number, y: number, z: number);
    abstract getTileData(layer: string): any;
}
