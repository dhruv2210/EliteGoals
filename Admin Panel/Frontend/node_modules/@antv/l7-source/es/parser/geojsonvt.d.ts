import { Feature, FeatureCollection, Geometries, Properties } from '@turf/helpers';
import { IParserData } from '../interface';
import { ITileParserCFG } from '@antv/l7-core';
export type MapboxVectorTile = {
    layers: {
        [key: string]: {
            features: Feature[];
        };
    };
};
export default function geojsonVTTile(data: FeatureCollection<Geometries, Properties>, cfg: ITileParserCFG): IParserData;
