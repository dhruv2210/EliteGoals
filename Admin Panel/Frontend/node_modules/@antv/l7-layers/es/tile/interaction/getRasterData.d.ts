import { IMapService, IRendererService } from '@antv/l7-core';
import { SourceTile } from '@antv/l7-utils';
export declare function readRasterValue(tile: SourceTile, mapService: IMapService, x: number, y: number): any;
export declare function readPixel(x: number, y: number, rendererService: IRendererService): false | Uint8Array;
