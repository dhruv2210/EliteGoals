import { ILayer } from '@antv/l7-core';
import Tile from './Tile';
export default class VectorTile extends Tile {
    initTileLayer(): Promise<void>;
    protected getSourceOption(): {
        data: {
            type: string;
            features: any;
        };
        options: {
            parser: {
                type: string;
                featureId: string;
            };
            transforms: import("@antv/l7-core").ITransform[];
        };
    };
    protected setLayerMinMaxZoom(layer: ILayer): void;
    getFeatures(sourceLayer: string): any;
    /**
     * 在一个 Tile 中可能存在一个相同 ID 的 feature
     * @param id
     * @returns
     */
    getFeatureById(id: number): import("@antv/l7-core").IParseDataItem[];
}
