import { ILayer, IPass, IPostProcessingPass } from '@antv/l7-core';
/**
 * 'blurH' -> ['blurH', {}]
 */
export declare function normalizePasses(passes: Array<string | [string, {
    [key: string]: unknown;
}]>): [string, {
    [key: string]: unknown;
}][];
/**
 * 默认添加 ClearPass、RenderPass
 * 以及 PostProcessing 中的最后一个 CopyPass
 */
export declare function createMultiPassRenderer(layer: ILayer, passes: Array<string | [string, {
    [key: string]: unknown;
}]>, postProcessingPassFactory: (name: string) => IPostProcessingPass<unknown>, normalPassFactory: (name: string) => IPass<unknown>): import("@antv/l7-core").IMultiPassRenderer;
