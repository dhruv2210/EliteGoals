export declare function createRendererContainer(domId: string | HTMLDivElement): HTMLDivElement | null;
/**
 * 检测触发事件是否是在 marker/popup 上发生，若是则会发生冲突（发生冲突时 marker/popup 事件优先）
 * @param layerEvent
 * @returns
 */
export declare function isEventCrash(layerEvent: any): boolean;
