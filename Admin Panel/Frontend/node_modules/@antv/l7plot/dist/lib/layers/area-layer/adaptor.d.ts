import { ILayer } from '../../types';
import { AreaLayerActiveOptions, AreaLayerOptions } from './types';
export declare const getDefaultState: (state?: AreaLayerOptions['state']) => {
    active: Required<AreaLayerActiveOptions>;
    select: Required<AreaLayerActiveOptions>;
};
export declare function mappingLayer(layer: ILayer, strokeLayer: ILayer, highlightLayer: ILayer, selectFillLayer: ILayer, selectStrokeLayer: ILayer, options: AreaLayerOptions): void;
