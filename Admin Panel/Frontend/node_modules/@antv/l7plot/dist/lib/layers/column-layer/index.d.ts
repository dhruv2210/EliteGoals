import { DotLayer } from '../dot-layer';
import { ColumnLayerOptions } from './types';
export type { ColumnLayerOptions };
export declare class ColumnLayer extends DotLayer<ColumnLayerOptions> {
    /**
     * 图层类型
     */
    type: import("../..").LayerType;
    /**
     * 获取默认配置
     */
    getDefaultOptions(): Partial<ColumnLayerOptions>;
}
