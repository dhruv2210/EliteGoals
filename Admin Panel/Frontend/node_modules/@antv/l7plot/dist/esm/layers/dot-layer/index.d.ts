import { PlotLayer } from '../../core/layer/plot-layer';
import { DotLayerOptions } from './types';
import { ILayer } from '../../types';
export type { DotLayerOptions };
export declare class DotLayer<O extends DotLayerOptions = DotLayerOptions> extends PlotLayer<O> {
    /**
     * 默认配置项
     */
    static DefaultOptions: {
        source: {
            data: never[];
            parser: {
                type: string;
                x: string;
                y: string;
            };
        };
        shape: string;
        size: number;
        color: string;
    };
    /**
     * 图层配置项 Keys
     */
    static LayerOptionsKeys: string[];
    /**
     * 图层名称
     */
    name: string;
    /**
     * 图层实例
     */
    layer: ILayer;
    /**
     * 图层类型
     */
    type: import("../../types").LayerType;
    /**
     * 图层是否具有交互属性
     */
    interaction: boolean;
    constructor(options: O);
    /**
     * 获取默认配置
     */
    getDefaultOptions(): Partial<O>;
    protected mappingLayer(layer: ILayer, options: O): void;
    update(options: Partial<O>): void;
    getColorLegendItems(): import("@antv/l7plot-component").CategoryLegendListItem[];
}
