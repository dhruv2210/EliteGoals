import { __assign, __extends } from "tslib";
import { pick } from '@antv/util';
import { Plot } from '../../core/plot';
import { DEFAULT_OPTIONS } from './constants';
import { HexbinLayer } from '../../layers/hexbin-layer';
import { LayerGroup } from '../../core/layer/layer-group';
var Hexbin = /** @class */ (function (_super) {
    __extends(Hexbin, _super);
    function Hexbin() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /**
         * 地图类型
         */
        _this.type = Plot.PlotType.Hexbin;
        return _this;
    }
    /**
     * 获取默认配置
     */
    Hexbin.prototype.getDefaultOptions = function () {
        return Hexbin.DefaultOptions;
    };
    /**
     * 创建图层
     */
    Hexbin.prototype.createLayers = function (source) {
        this.hexbinLayer = new HexbinLayer(__assign({ source: source }, pick(this.options, HexbinLayer.LayerOptionsKeys)));
        var layerGroup = new LayerGroup([this.hexbinLayer]);
        if (this.options.label) {
            this.labelLayer = this.createLabelLayer(this.source, this.options.label, this.options);
            layerGroup.addLayer(this.labelLayer);
        }
        return layerGroup;
    };
    /**
     * 更新图层
     */
    Hexbin.prototype.updateLayers = function (options) {
        var heatMapLayerConfig = pick(options, HexbinLayer.LayerOptionsKeys);
        this.hexbinLayer.update(heatMapLayerConfig);
        this.updateLabelLayer(this.source, options.label, this.options, this.labelLayer);
    };
    /**
     * 默认配置项
     */
    Hexbin.DefaultOptions = DEFAULT_OPTIONS;
    return Hexbin;
}(Plot));
export { Hexbin };
//# sourceMappingURL=index.js.map