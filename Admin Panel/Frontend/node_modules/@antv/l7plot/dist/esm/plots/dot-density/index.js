import { __assign, __extends } from "tslib";
import { pick } from '@antv/util';
import { Plot } from '../../core/plot';
import { DEFAULT_OPTIONS } from './constants';
import { LayerGroup } from '../../core/layer/layer-group';
import { DotDensityLayer } from '../../layers/dot-density-layer';
var DotDensity = /** @class */ (function (_super) {
    __extends(DotDensity, _super);
    function DotDensity() {
        var _this = _super !== null && _super.apply(this, arguments) || this;
        /**
         * 地图类型
         */
        _this.type = Plot.PlotType.DotDensity;
        return _this;
    }
    /**
     * 获取默认配置
     */
    DotDensity.prototype.getDefaultOptions = function () {
        return DotDensity.DefaultOptions;
    };
    /**
     * 创建图层
     */
    DotDensity.prototype.createLayers = function (source) {
        this.dotDensityLayer = new DotDensityLayer(__assign({ source: source }, pick(this.options, DotDensityLayer.LayerOptionsKeys)));
        var layerGroup = new LayerGroup([this.dotDensityLayer]);
        return layerGroup;
    };
    /**
     * 更新图层
     */
    DotDensity.prototype.updateLayers = function (options) {
        var bubbleLayerConfig = pick(options, DotDensityLayer.LayerOptionsKeys);
        this.dotDensityLayer.update(bubbleLayerConfig);
    };
    /**
     * 默认配置项
     */
    DotDensity.DefaultOptions = DEFAULT_OPTIONS;
    return DotDensity;
}(Plot));
export { DotDensity };
//# sourceMappingURL=index.js.map