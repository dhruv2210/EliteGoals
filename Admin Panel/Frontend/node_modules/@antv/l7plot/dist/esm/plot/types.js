var _a, _b;
import { LayerType, PlotType } from '../types';
import { Dot } from '../plots/dot';
import { DotDensity } from '../plots/dot-density';
import { Heatmap } from '../plots/heatmap';
import { Grid } from '../plots/grid';
import { Hexbin } from '../plots/hexbin';
import { Path } from '../plots/path';
import { Flow } from '../plots/flow';
import { Area } from '../plots/area';
import { Choropleth } from '../plots/choropleth';
import { TextLayer } from '../layers/text-layer';
import { DotLayer } from '../layers/dot-layer';
import { IconLayer } from '../layers/icon-layer';
import { ColumnLayer } from '../layers/column-layer';
import { DotDensityLayer } from '../layers/dot-density-layer';
import { GridLayer } from '../layers/grid-layer';
import { HexbinLayer } from '../layers/hexbin-layer';
import { PathLayer } from '../layers/path-layer';
import { ArcLayer } from '../layers/arc-layer';
import { HeatmapLayer } from '../layers/heatmap-layer';
import { AreaLayer } from '../layers/area-layer';
import { PrismLayer } from '../layers/prism-layer';
/**
 * plots 的 class
 */
export var PLOTS_MAP = (_a = {},
    _a[PlotType.Dot] = Dot,
    _a[PlotType.DotDensity] = DotDensity,
    _a[PlotType.Heatmap] = Heatmap,
    _a[PlotType.Grid] = Grid,
    _a[PlotType.Hexbin] = Hexbin,
    _a[PlotType.Path] = Path,
    _a[PlotType.Flow] = Flow,
    _a[PlotType.Area] = Area,
    _a[PlotType.Choropleth] = Choropleth,
    _a);
/**
 * L7Plot 内置的 layers 的 class
 */
export var LAYERS_MAP = (_b = {},
    _b[LayerType.TextLayer] = TextLayer,
    _b[LayerType.DotLayer] = DotLayer,
    _b[LayerType.IconLayer] = IconLayer,
    _b[LayerType.DotDensity] = DotDensityLayer,
    _b[LayerType.ColumnLayer] = ColumnLayer,
    _b[LayerType.HeatmapLayer] = HeatmapLayer,
    _b[LayerType.GridLayer] = GridLayer,
    _b[LayerType.HexbinLayer] = HexbinLayer,
    _b[LayerType.PathLayer] = PathLayer,
    _b[LayerType.ArcLayer] = ArcLayer,
    _b[LayerType.AreaLayer] = AreaLayer,
    _b[LayerType.PrismLayer] = PrismLayer,
    _b);
//# sourceMappingURL=types.js.map