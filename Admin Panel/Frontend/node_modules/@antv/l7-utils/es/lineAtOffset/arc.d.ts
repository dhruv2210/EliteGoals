import { Version } from '../interface/map';
import { Point } from './interface';
export declare function arcLineAtOffset(source: Point, target: Point, offset: number, thetaOffset: number | undefined, mapVersion: Version | undefined, segmentNumber: number | undefined, autoFit: boolean): number[];
