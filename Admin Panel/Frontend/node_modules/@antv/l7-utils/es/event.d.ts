export declare function bindAll(fns: string[], context: any): void;
export declare class FrequencyController {
    private duration;
    private timestamp;
    constructor(duration?: number);
    run(callback: () => any): void;
}
