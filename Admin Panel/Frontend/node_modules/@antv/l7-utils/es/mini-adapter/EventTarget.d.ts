import { Event } from './Event';
export default class EventTarget {
    constructor();
    addEventListener(type: any, listener: any, options?: any): void;
    removeEventListener(type: any, listener: any, options?: {}): void;
    dispatchEvent(event: Event): void;
}
