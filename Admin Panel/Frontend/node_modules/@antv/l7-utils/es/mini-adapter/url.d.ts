import { Blob } from './blob';
export declare class URL {
    /**
     * fake createObject, use base64 instead
     * @param blob
     */
    static createObjectURL(blob: Blob): string;
    href: string;
    constructor(url: any, host?: string);
}
