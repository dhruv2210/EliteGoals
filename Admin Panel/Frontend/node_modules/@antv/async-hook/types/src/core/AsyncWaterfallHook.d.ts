import { CallBack } from './IHook';
export default class AsyncWaterfallHook {
    private tasks;
    constructor(...args: any[]);
    promise(...args: any[]): Promise<any>;
    tapPromise(name: string, cb: CallBack): void;
}
